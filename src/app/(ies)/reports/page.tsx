import ProjectMargins from "./ProjectMargins";

export const dynamic = "force-dynamic";
export const metadata = { title: "Project margins · Footnote for IES" };

export default function ReportsPage() {
  return <ProjectMargins />;
}
