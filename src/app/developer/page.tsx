import DevWorkspace from "./DevWorkspace";

export const dynamic = "force-dynamic";
export const metadata = { title: "Agent platform · Footnote for IES" };

export default function DeveloperPage() {
  return <DevWorkspace />;
}
